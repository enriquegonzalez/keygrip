<%= form_for(@crew_member_skill) do |f| %>
  <% if @crew_member_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@crew_member_skill.errors.count, "error") %> prohibited this crew_member_skill from being saved:</h2>

      <ul>
      <% @crew_member_skill.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :crew_member_id %><br />
    <%= f.number_field :crew_member_id %>
  </div>
  <div class="field">
    <%= f.label :skill_id %><br />
    <%= f.number_field :skill_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
